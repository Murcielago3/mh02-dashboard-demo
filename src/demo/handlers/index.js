// Registers every mock route. Imported once (for its side effects) by
// src/api/client.js, before any api module issues a request.
import './core'
import './projects'
import './hr'
import './finance'
